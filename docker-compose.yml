version: "3.9"

services:
  app:
    build: .
    container_name: smartguard-web

    restart: unless-stopped

    ports:
      - "3000:3000"

    environment:
      NODE_ENV: production
      NITRO_PRESET: node-server
      HOST: 0.0.0.0
      PORT: 3000

    volumes:
      - ./.env:/app/.env:ro