version: "3.9"

services:
  web:
    build: .
    container_name: smartguard-web
    restart: unless-stopped
    ports:
      - "3010:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
