services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartguard-web
    ports:
      - "3010:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
