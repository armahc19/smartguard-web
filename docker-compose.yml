version: "3.9"

services:
  web:
    build: .
    container_name: smartguard-web
    restart: unless-stopped
    ports:
      - "3010:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
    # Temporarily comment this out to isolate if your local .env is causing the crash
    # volumes:
    #   - ./.env:/app/.env:ro
